# Node.js dependencies
# Typically large and OS-specific, should be installed via npm/yarn/pnpm
/node_modules

# Logs & Debug Files
# Log files and debug outputs generated during development or runtime
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json
pid.[0-9]*.log
pid.[0-9]*.log.[0-9]*
*.pid
*.seed
*.pid.lock

# Environment Variables
# Never commit sensitive environment variables (API keys, secrets, etc.)
.env
.env.*
!.env.example

# Build output directories (optional, good practice if you ever add a build step)
/dist
/build
/out
/.parcel-cache

# Project-specific generated files
# Ignore the screenshots generated by the script
/screenshots/

# OS generated files
# Ignore system files generated by macOS, Windows, Linux
.DS_Store
Thumbs.db
._*

# Editor directories and files
# Ignore settings specific to IDEs/editors
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.sublime-workspace
*.sublime-project
.idea/
# Add other editor ignores if needed (.atom/, etc.)

# Test output (if you add tests later)
/coverage
/.nyc_output
*.lcov