"AD Overlay GSI"
{
    "uri"           "http://127.0.0.1:3001"  // The address your local server will listen on
    "timeout"       "5.0"
    "buffer"        "0.1"
    "throttle"      "0.1"                    // How often to potentially send updates
    "heartbeat"     "30.0"
    "data"
    {
        "provider"      "1"
        "map"           "1"  // Include map state (gamestate, draft phase?)
        "player"        "1"  // Include player state
        "hero"          "1"  // Include hero state
        "abilities"     "1"  // Crucial: Include ability state (levels, cooldowns)
        "items"         "1"  // Optional: Include items if needed
        "draft"         "1"  // Unlikely to provide detailed AD picks, but include for testing
    }
}